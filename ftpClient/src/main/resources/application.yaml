spring:
    datasource:
        driver-class-name: org.postgresql.Driver
        password: postgres
        username: postgres
        url: jdbc:postgresql://localhost:5432/postgres
    jpa:
        hibernate:
            ddl-auto: create

ftp:
    host: localhost
    user: demo
    pass: mysecretpassword123
    port: 21
    client:
        mode: 2 #ACTIVE_LOCAL_DATA_CONNECTION_MODE = 0, PASSIVE_LOCAL_DATA_CONNECTION_MODE = 2
    remote:
        directory: "/home/demo"
    local:
        directory: "src/main/resources/ftp/local"

logging:
    level:
        org.springframework: ERROR
        com.example: INFO